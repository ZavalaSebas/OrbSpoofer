<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:sys="clr-namespace:System;assembly=mscorlib">

    <!-- OrbSpoofer Fluent Theme — structure + semantic colors only.
         Palette lives in Themes/DarkTheme.xaml + LightTheme.xaml (same keys).
         NOTE: never share accent Freezables here — a shared brush freezes on first
         StaticResource use and its DynamicResource stops go deaf. Inline accent
         gradients per element instead. -->
    <LinearGradientBrush x:Key="Orb.Play.Gradient" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="#57F287" Offset="0"/>
        <GradientStop Color="#3DD67A" Offset="1"/>
    </LinearGradientBrush>
    <SolidColorBrush x:Key="Orb.PlayBrush" Color="#57F287" />
    <SolidColorBrush x:Key="Orb.StopBrush" Color="#ED4245" />

    <!-- WPF-UI compat brushes — DynamicResource so they follow theme + accent live -->
    <SolidColorBrush x:Key="ApplicationBackgroundBrush" Color="{DynamicResource Orb.Background.Primary.Color}" />
    <SolidColorBrush x:Key="SolidBackgroundFillColorBaseBrush" Color="{DynamicResource Orb.Background.Primary.Color}" />
    <SolidColorBrush x:Key="SolidBackgroundFillColorSecondaryBrush" Color="{DynamicResource Orb.Background.Secondary.Color}" />
    <SolidColorBrush x:Key="SolidBackgroundFillColorTertiaryBrush" Color="{DynamicResource Orb.Background.Tertiary.Color}" />
    <SolidColorBrush x:Key="TextFillColorPrimaryBrush" Color="{DynamicResource Orb.Text.Primary.Color}" />
    <SolidColorBrush x:Key="TextFillColorSecondaryBrush" Color="{DynamicResource Orb.Text.Secondary.Color}" />
    <SolidColorBrush x:Key="TextFillColorTertiaryBrush" Color="{DynamicResource Orb.Text.Muted.Color}" />
    <SolidColorBrush x:Key="ControlFillColorDefaultBrush" Color="{DynamicResource Orb.Background.Tertiary.Color}" />
    <SolidColorBrush x:Key="ControlStrokeColorDefaultBrush" Color="{DynamicResource Orb.SeparatorBrush}" />
    <SolidColorBrush x:Key="AccentFillColorDefaultBrush" Color="{DynamicResource SystemAccentColorPrimary}" />
    <SolidColorBrush x:Key="AccentFillColorSecondaryBrush" Color="{DynamicResource SystemAccentColorPrimary}" />
    <SolidColorBrush x:Key="AccentFillColorTertiaryBrush" Color="{DynamicResource SystemAccentColorPrimary}" />

    <FontFamily x:Key="Orb.FontFamily">Segoe UI Variable, Segoe UI</FontFamily>
    <sys:Double x:Key="Orb.FontSize.Caption">11</sys:Double>
    <sys:Double x:Key="Orb.FontSize.Body">13</sys:Double>
    <sys:Double x:Key="Orb.FontSize.Title">20</sys:Double>
    <sys:Double x:Key="Orb.FontSize.Display">28</sys:Double>

    <Thickness x:Key="Orb.Spacing.XS">4</Thickness>
    <Thickness x:Key="Orb.Spacing.SM">8</Thickness>
    <Thickness x:Key="Orb.Spacing.MD">12</Thickness>
    <Thickness x:Key="Orb.Spacing.LG">16</Thickness>
    <CornerRadius x:Key="Orb.CornerRadius.Card">12</CornerRadius>
    <CornerRadius x:Key="Orb.CornerRadius.Control">8</CornerRadius>

    <!-- Motion — professional, smooth -->
    <Duration x:Key="Orb.Motion.Fast">0:0:0.18</Duration>
    <Duration x:Key="Orb.Motion.Normal">0:0:0.28</Duration>
    <Duration x:Key="Orb.Motion.Slow">0:0:0.40</Duration>
    <Duration x:Key="Orb.Motion.ExtraSlow">0:0:0.55</Duration>
    <CubicEase x:Key="Orb.Ease.OutCubic" EasingMode="EaseOut"/>
    <CubicEase x:Key="Orb.Ease.InOutCubic" EasingMode="EaseInOut"/>
    <KeySpline x:Key="Orb.Easing.EaseOut">0.22,1,0.36,1</KeySpline>
    <KeySpline x:Key="Orb.Easing.Emphasized">0.2,0,0,1</KeySpline>
    <KeySpline x:Key="Orb.Easing.Standard">0.4,0,0.2,1</KeySpline>

    <!-- NOTE: palette brushes (Orb.Card.*, Orb.SeparatorBrush, glows, header/sidebar
         gradients) live in the theme dictionaries so Light/Dark can differ. -->

    <!-- ===== Cards ===== -->
    <Style x:Key="Orb.Card" TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource Orb.Card.Background}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Orb.SeparatorBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="{StaticResource Orb.CornerRadius.Card}" />
        <Setter Property="Padding" Value="14" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
        <Setter Property="RenderTransform">
            <Setter.Value>
                <TransformGroup>
                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                    <TranslateTransform X="0" Y="0"/>
                </TransformGroup>
            </Setter.Value>
        </Setter>
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect BlurRadius="12" ShadowDepth="0" Opacity="0.18" Color="#000000"/>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Animated card — hover lift + accent border + shadow (no overflow).
         Colors are instant Setters (not ColorAnimations) because animation targets
         can't bind DynamicResource — this keeps hover theme-aware in Light/Dark. -->
    <Style x:Key="Orb.CardHover" TargetType="Border" BasedOn="{StaticResource Orb.Card}">
        <Setter Property="Panel.ZIndex" Value="0"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Panel.ZIndex" Value="10"/>
                <Setter Property="BorderBrush" Value="{DynamicResource SystemAccentColorSecondaryBrush}"/>
                <Setter Property="Background" Value="{DynamicResource Orb.Card.Hover}"/>
                <Trigger.EnterActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="(Border.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleX)" To="1.006" Duration="{StaticResource Orb.Motion.Fast}">
                                <DoubleAnimation.EasingFunction><CubicEase EasingMode="EaseOut"/></DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                            <DoubleAnimation Storyboard.TargetProperty="(Border.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleY)" To="1.006" Duration="{StaticResource Orb.Motion.Fast}">
                                <DoubleAnimation.EasingFunction><CubicEase EasingMode="EaseOut"/></DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                            <DoubleAnimation Storyboard.TargetProperty="(Border.RenderTransform).(TransformGroup.Children)[1].(TranslateTransform.Y)" To="-2" Duration="{StaticResource Orb.Motion.Fast}">
                                <DoubleAnimation.EasingFunction><CubicEase EasingMode="EaseOut"/></DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.EnterActions>
                <Trigger.ExitActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="(Border.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleX)" To="1" Duration="{StaticResource Orb.Motion.Fast}"/>
                            <DoubleAnimation Storyboard.TargetProperty="(Border.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleY)" To="1" Duration="{StaticResource Orb.Motion.Fast}"/>
                            <DoubleAnimation Storyboard.TargetProperty="(Border.RenderTransform).(TransformGroup.Children)[1].(TranslateTransform.Y)" To="0" Duration="{StaticResource Orb.Motion.Fast}"/>
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.ExitActions>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Card con entrada animada (para stagger via Loaded) -->
    <Style x:Key="Orb.CardAnimated" TargetType="Border" BasedOn="{StaticResource Orb.CardHover}">
        <Setter Property="Opacity" Value="0"/>
        <Setter Property="RenderTransform">
            <Setter.Value>
                <TransformGroup>
                    <ScaleTransform ScaleX="0.98" ScaleY="0.98"/>
                    <TranslateTransform X="0" Y="8"/>
                </TransformGroup>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <EventTrigger RoutedEvent="Loaded">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.35">
                            <DoubleAnimation.EasingFunction><CubicEase EasingMode="EaseOut"/></DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                        <DoubleAnimation Storyboard.TargetProperty="(Border.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleX)" From="0.98" To="1" Duration="0:0:0.35">
                            <DoubleAnimation.EasingFunction><CubicEase EasingMode="EaseOut"/></DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                        <DoubleAnimation Storyboard.TargetProperty="(Border.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleY)" From="0.98" To="1" Duration="0:0:0.35">
                            <DoubleAnimation.EasingFunction><CubicEase EasingMode="EaseOut"/></DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                        <DoubleAnimation Storyboard.TargetProperty="(Border.RenderTransform).(TransformGroup.Children)[1].(TranslateTransform.Y)" From="8" To="0" Duration="0:0:0.40">
                            <DoubleAnimation.EasingFunction><CubicEase EasingMode="EaseOut"/></DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Style.Triggers>
    </Style>

    <!-- View host — fade + slide -->
    <Style x:Key="Orb.ViewHost" TargetType="Grid">
        <Setter Property="Opacity" Value="0"/>
        <Setter Property="RenderTransform">
            <Setter.Value>
                <TranslateTransform X="0" Y="12"/>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="Visibility" Value="Visible">
                <Trigger.EnterActions>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.32">
                                <DoubleAnimation.EasingFunction><CubicEase EasingMode="EaseOut"/></DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                            <DoubleAnimation Storyboard.TargetProperty="(Grid.RenderTransform).(TranslateTransform.Y)" From="12" To="0" Duration="0:0:0.38">
                                <DoubleAnimation.EasingFunction><CubicEase EasingMode="EaseOut"/></DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                        </Storyboard>
                    </BeginStoryboard>
                </Trigger.EnterActions>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Sidebar button — accent slide indicator -->
    <Style x:Key="Orb.SidebarButton" TargetType="Border">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="CornerRadius" Value="8"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource Orb.Card.Hover}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Play button press -->
    <Style x:Key="Orb.PlayButton" TargetType="Border">
        <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
        <Setter Property="RenderTransform"><Setter.Value><ScaleTransform ScaleX="1" ScaleY="1"/></Setter.Value></Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Trigger.EnterActions>
                    <BeginStoryboard><Storyboard>
                        <DoubleAnimation Storyboard.TargetProperty="(Border.RenderTransform).(ScaleTransform.ScaleX)" To="1.04" Duration="0:0:0.14"/>
                        <DoubleAnimation Storyboard.TargetProperty="(Border.RenderTransform).(ScaleTransform.ScaleY)" To="1.04" Duration="0:0:0.14"/>
                    </Storyboard></BeginStoryboard>
                </Trigger.EnterActions>
                <Trigger.ExitActions>
                    <BeginStoryboard><Storyboard>
                        <DoubleAnimation Storyboard.TargetProperty="(Border.RenderTransform).(ScaleTransform.ScaleX)" To="1" Duration="0:0:0.14"/>
                        <DoubleAnimation Storyboard.TargetProperty="(Border.RenderTransform).(ScaleTransform.ScaleY)" To="1" Duration="0:0:0.14"/>
                    </Storyboard></BeginStoryboard>
                </Trigger.ExitActions>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Scrollbar — thin thumb, accent hover. Thumb colors are theme tokens. -->
    <Style TargetType="ScrollBar">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Width" Value="10"/>
        <Setter Property="MinWidth" Value="10"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ScrollBar">
                    <Grid Background="Transparent">
                        <Track x:Name="PART_Track" IsDirectionReversed="True" VerticalAlignment="Stretch">
                            <Track.DecreaseRepeatButton><RepeatButton Command="{x:Static ScrollBar.PageUpCommand}" Focusable="False" IsTabStop="False" Opacity="0"/></Track.DecreaseRepeatButton>
                            <Track.IncreaseRepeatButton><RepeatButton Command="{x:Static ScrollBar.PageDownCommand}" Focusable="False" IsTabStop="False" Opacity="0"/></Track.IncreaseRepeatButton>
                            <Track.Thumb>
                                <Thumb Focusable="False" IsTabStop="False">
                                    <Thumb.Template>
                                        <ControlTemplate TargetType="Thumb">
                                            <!-- Accent thumb (follows theme) over neutral base; hover fades it in.
                                                 Opacity animation is used because ColorAnimation.To can't bind DynamicResource. -->
                                            <Grid>
                                                <Border CornerRadius="5" Margin="2,1" Background="{DynamicResource Orb.Scrollbar.ThumbBrush}" Opacity="0.9"/>
                                                <Border x:Name="ThumbAccent" CornerRadius="5" Margin="2,1" Background="{DynamicResource Orb.SystemAccentBrush}" Opacity="0">
                                                    <Border.Triggers>
                                                        <EventTrigger RoutedEvent="MouseEnter">
                                                            <BeginStoryboard><Storyboard>
                                                                <DoubleAnimation Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.18"/>
                                                            </Storyboard></BeginStoryboard>
                                                        </EventTrigger>
                                                        <EventTrigger RoutedEvent="MouseLeave">
                                                            <BeginStoryboard><Storyboard>
                                                                <DoubleAnimation Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.18"/>
                                                            </Storyboard></BeginStoryboard>
                                                        </EventTrigger>
                                                    </Border.Triggers>
                                                </Border>
                                            </Grid>
                                        </ControlTemplate>
                                    </Thumb.Template>
                                </Thumb>
                            </Track.Thumb>
                        </Track>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="HorizontalScrollBar" TargetType="ScrollBar">
        <Setter Property="Background" Value="Transparent"/><Setter Property="Height" Value="10"/><Setter Property="MinHeight" Value="10"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ScrollBar">
                    <Grid Background="Transparent">
                        <Track x:Name="PART_Track" VerticalAlignment="Stretch">
                            <Track.DecreaseRepeatButton><RepeatButton Command="{x:Static ScrollBar.PageLeftCommand}" Focusable="False" IsTabStop="False" Opacity="0"/></Track.DecreaseRepeatButton>
                            <Track.IncreaseRepeatButton><RepeatButton Command="{x:Static ScrollBar.PageRightCommand}" Focusable="False" IsTabStop="False" Opacity="0"/></Track.IncreaseRepeatButton>
                            <Track.Thumb><Thumb Focusable="False" IsTabStop="False"><Thumb.Template><ControlTemplate TargetType="Thumb"><Border CornerRadius="5" Margin="2,1" Background="{DynamicResource Orb.Scrollbar.ThumbBrush}" Opacity="0.9"/></ControlTemplate></Thumb.Template></Thumb></Track.Thumb>
                        </Track>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ListBoxItem — Fluent selection with subtle animation -->
    <Style TargetType="ListBoxItem">
        <Setter Property="Background" Value="Transparent"/><Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/><Setter Property="Padding" Value="0"/><Setter Property="BorderThickness" Value="0"/><Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListBoxItem">
                    <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6" Padding="{TemplateBinding Padding}" Margin="0,0,0,4">
                        <ContentPresenter/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="border" Property="Background" Value="{DynamicResource Orb.Card.Hover}"/></Trigger>
                        <Trigger Property="IsSelected" Value="True"><Setter TargetName="border" Property="Background" Value="Transparent"/><Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource SystemAccentColorPrimaryBrush}"/><Setter Property="Foreground" Value="{DynamicResource PrimaryBrush}"/></Trigger>
                        <MultiTrigger><MultiTrigger.Conditions><Condition Property="IsSelected" Value="True"/><Condition Property="IsMouseOver" Value="True"/></MultiTrigger.Conditions><Setter TargetName="border" Property="Background" Value="{DynamicResource Orb.Card.Hover}"/></MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ContextMenu polish — more compact, less plastic -->
    <Style TargetType="ContextMenu">
        <Setter Property="Background" Value="{DynamicResource SurfaceBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
        <Setter Property="Padding" Value="6" />
        <Setter Property="HasDropShadow" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ContextMenu">
                    <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="12" Padding="{TemplateBinding Padding}">
                        <Border.Effect><DropShadowEffect BlurRadius="20" ShadowDepth="6" Opacity="0.45" Color="#000000"/></Border.Effect>
                        <ScrollViewer VerticalScrollBarVisibility="Auto"><ItemsPresenter/></ScrollViewer>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style TargetType="MenuItem">
        <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
        <Setter Property="Padding" Value="10,7" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="MenuItem">
                    <Grid>
                        <Border x:Name="Bd" Background="Transparent" CornerRadius="6" Padding="{TemplateBinding Padding}"/>
                        <Grid Margin="{TemplateBinding Padding}">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="22"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                            <ContentPresenter Grid.Column="0" ContentSource="Icon" HorizontalAlignment="Center"/>
                            <ContentPresenter Grid.Column="1" ContentSource="Header" Margin="6,0,12,0"/>
                        </Grid>
                        <Popup x:Name="PART_Popup" Placement="Right" AllowsTransparency="True" IsOpen="{Binding IsSubmenuOpen, RelativeSource={RelativeSource TemplatedParent}}" PopupAnimation="Fade">
                            <Border Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="12" Padding="6"><ItemsPresenter/></Border>
                        </Popup>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsHighlighted" Value="True"><Setter TargetName="Bd" Property="Background" Value="{DynamicResource Orb.ElevatedBrush}"/></Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
