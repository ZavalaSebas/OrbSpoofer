<UserControl x:Class="OrbSpoofer.Views.DatabaseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
            <Border Width="36" Height="36" CornerRadius="9" VerticalAlignment="Center" Margin="0,0,12,0" Background="#1E2230">
                <ui:SymbolIcon Symbol="Database24" FontSize="18" Foreground="#7B83FF" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                <TextBlock Text="Discord Database" FontSize="19" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}"/>
                <TextBlock FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" Text="{Binding DbSourceText}" Margin="0,2,0,0"/>
            </StackPanel>
        </Grid>

        <Border Grid.Row="1" Margin="0,14,0,14" Background="#0F0F11" CornerRadius="12" Padding="4" BorderBrush="#1E1E22" BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                <ui:SymbolIcon Grid.Column="0" Symbol="Search24" FontSize="16" Foreground="{StaticResource TextMutedBrush}" Margin="10,0,6,0" VerticalAlignment="Center"/>
                <TextBox Grid.Column="1" Background="Transparent" BorderThickness="0" Foreground="{StaticResource TextPrimaryBrush}" FontSize="13" VerticalContentAlignment="Center" Padding="0,10"
                         Text="{Binding Query, UpdateSourceTrigger=PropertyChanged}" BorderBrush="Transparent">
                    <TextBox.Style>
                        <Style TargetType="TextBox">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="TextBox">
                                        <Grid>
                                            <ScrollViewer x:Name="PART_ContentHost" VerticalAlignment="Center"/>
                                            <TextBlock Text="Search games..." Foreground="{StaticResource TextMutedBrush}" VerticalAlignment="Center" IsHitTestVisible="False" Visibility="Collapsed" x:Name="Ph"/>
                                        </Grid>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="Text" Value=""><Setter TargetName="Ph" Property="Visibility" Value="Visible"/></Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </TextBox.Style>
                </TextBox>
                <Button Grid.Column="2" Content="Search" Padding="18,8" Margin="4,2,2,2" Cursor="Hand" FontWeight="SemiBold" FontSize="12" Foreground="White" Command="{Binding SearchCommand}" BorderThickness="0">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border CornerRadius="8" Padding="{TemplateBinding Padding}" Background="{StaticResource Orb.Accent.Gradient}">
                                <Border.Effect><DropShadowEffect BlurRadius="8" ShadowDepth="1" Opacity="0.28" Color="#5865F2"/></Border.Effect>
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>
        </Border>

        <Grid Grid.Row="2">
            <ListBox x:Name="ResultsList" Padding="4,4,8,6" ClipToBounds="False" ScrollViewer.VerticalScrollBarVisibility="Auto"
                     Background="Transparent" BorderThickness="0" ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     ItemsSource="{Binding Results}" VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling" ScrollViewer.CanContentScroll="True" ScrollViewer.IsDeferredScrollingEnabled="True" MouseDoubleClick="ResultsList_MouseDoubleClick">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="ClipToBounds" Value="False"/>
                        <Setter Property="Panel.ZIndex" Value="0"/>
                        <Setter Property="Margin" Value="0,1,0,1"/>
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border Background="Transparent" ClipToBounds="False"><ContentPresenter/></Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers><Trigger Property="IsMouseOver" Value="True"><Setter Property="Panel.ZIndex" Value="10"/></Trigger></Style.Triggers>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource Orb.CardHover}" Margin="4,2,12,6" ClipToBounds="False" ToolTip="Double-click to spoof">
                            <Grid>
                                <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                <Border Grid.Column="0" Width="78" Height="42" CornerRadius="9" Background="#0A0A0C" Margin="0,0,14,0" ClipToBounds="True" BorderBrush="#1E1E22" BorderThickness="1">
                                    <Grid>
                                        <TextBlock Text="??" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#2A2A30"/>
                                        <Image Source="{Binding ImageUrl}" Width="78" Height="42" Stretch="UniformToFill" RenderOptions.BitmapScalingMode="HighQuality"/>
                                    </Grid>
                                </Border>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Name}" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}"/>
                                    <TextBlock FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,3,0,0">
                                        <Run Text="ID: "/><Run Text="{Binding Id, Mode=OneWay}" Foreground="{StaticResource TextMutedBrush}"/>
                                    </TextBlock>
                                    <TextBlock FontSize="11" Foreground="{StaticResource TextMutedBrush}" Margin="0,2,0,0" Text="{Binding AliasDisplay}" TextTrimming="CharacterEllipsis"/>
                                </StackPanel>
                                <Button Grid.Column="2" Command="{Binding DataContext.SpoofCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" ToolTip="Spoof" Cursor="Hand" Width="42" Height="42" BorderThickness="0" Background="Transparent" Padding="0">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Border CornerRadius="10" Width="42" Height="42" Background="{StaticResource Orb.Accent.Gradient}">
                                                <Border.Effect><DropShadowEffect BlurRadius="8" ShadowDepth="1" Opacity="0.28" Color="#5865F2"/></Border.Effect>
                                                <ui:SymbolIcon Symbol="Play24" Filled="True" Foreground="White" FontSize="15" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Button.Template>
                                </Button>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="{Binding HasNoResults, Converter={StaticResource BoolToVisibilityConverter}}">
                <Border Width="56" Height="56" CornerRadius="14" Background="#1A1A1E" HorizontalAlignment="Center" BorderBrush="#23232A" BorderThickness="1">
                    <ui:SymbolIcon Symbol="Database24" FontSize="24" Foreground="{StaticResource TextMutedBrush}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <TextBlock Text="{Binding NoResultsText}" FontSize="14" Foreground="{StaticResource TextMutedBrush}" HorizontalAlignment="Center" Margin="0,12,0,0"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
