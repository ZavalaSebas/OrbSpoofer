<ui:FluentWindow x:Class="OrbSpoofer.UI.Windows.InfoDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="OrbSpoofer" Width="420"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        Background="Transparent"
        Foreground="{StaticResource TextPrimaryBrush}"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica"
        WindowCornerPreference="Round">
    <Grid Background="{StaticResource BackgroundBrush}" Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="OrbSpoofer" Height="32" Margin="-24,-24,-24,12" Background="{StaticResource SurfaceBrush}"/>

        <TextBlock x:Name="TitleText" Grid.Row="1" FontSize="18" FontWeight="SemiBold"
                   Foreground="{StaticResource WarningBrush}" Margin="0,0,0,12"/>

        <TextBlock x:Name="MessageText" Grid.Row="2" FontSize="13"
                   Foreground="{StaticResource TextSecondaryBrush}"
                   TextWrapping="Wrap" LineHeight="22" Margin="0,0,0,4"/>

        <ui:Button x:Name="OkButton" Grid.Row="3" Content="OK"
                Appearance="Primary"
                HorizontalAlignment="Right" Margin="0,16,0,0"
                Click="OkButton_Click"/>
    </Grid>
</ui:FluentWindow>
